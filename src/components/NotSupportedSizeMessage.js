function NotSupportedSizeMessage(params) {
  return (
    <div className="not-supported-size-message">
      This site is not supported at this size, try rotating your device or
      viewing on a larger device
    </div>
  )
}

export default NotSupportedSizeMessage
